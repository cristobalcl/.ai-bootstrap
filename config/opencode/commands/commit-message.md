---
description: Create commit message for current changes
agent: plan
---

Create a short phrase in English to use as commit message for these changes:

!`git status`
!`git diff`
!`git diff --staged`

It should start with "feat", "fix", "doc", or "chore", then a colon (':') and then the commit messages starting with lowercase, like:

"feat: added add task button"
